{
  "$schema": "./announcements.schema.md",
  "announcements": [
    {
      "id": "2026-09-26-v1.2.2-release",
      "title": "v1.2.2：一键升级已修复；思考档位按模型抬高；不可用模型不再进选择器",
      "level": "update",
      "pinned": true,
      "createdAt": "2026-09-26T02:00:00Z",
      "html": "<p><strong>Our Free Model v1.2.2</strong> 逐条处理了 issue 追踪单上的问题：</p><ul><li><strong>应用内升级恢复可用（#1）</strong>——1.2.1 的发布清单与实际文件不一致，任何旧版本点「一键升级」都会在校验阶段失败。清单已随版本重新生成，并新增 <code>build-manifest.mjs --check</code>：清单与实物不一致就非零退出，已接进 <code>npm test</code> 与 CI。</li><li><strong>思考关不掉的模型不再被频繁截断（#2）</strong>——MiMo V2.6 这类模型的思考与正文共用同一份额度，三档整体抬高为 4 096 / 16 384 / 模型上限；设置页每张模型卡与思考档位的说明文字都改为按真实下发的数字生成，不再出现「界面写 8K、实际发 16K」。</li><li><strong>探测判定不可用的模型不再进选择器（#3）</strong>——只有网关明确点名「这个 id 不路由」才移出下拉框，并在设置页保留拒因；5xx、限流、超时断网都不再被当作对模型的判定，全新安装里还没探测完的模型照常可选。</li><li><strong>无浏览器界面的 composition 也能装（#4）</strong>——插件只把模型车道当作硬依赖；设置页半身挂在它自己的 fiber 上，等 web 服务出现再挂载，所以在没有 web server 的环境里模型照样出得来。（真实 dsh-tui 环境尚未实机验证。）</li><li><strong>流式响应认出 body 而不是 header（#6）</strong>——网关高负载下会用 <code>application/json</code> 的 content-type 回一整套 SSE 帧；现在按响应体形状判定，已嗅探的字节会重新喂回流里，既不会整轮报错，也不会把能用的模型误判成不可用。</li></ul><p>旧版本用户请前往 <strong>设置 → 插件升级</strong> 一键升级；新安装用户无需任何操作。（#5 提出的多出口节点轮询不在实现范围内。）</p>"
    }
  ]
}
